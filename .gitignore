# 수집 이력 DB는 로컬에만 둔다.
# events.json 은 커밋한다 — 공공데이터포털이 해외 IP를 차단해
# GitHub Actions 에서는 수집이 안 되므로, 로컬 수집 결과를 올려 배포한다.
collector/events.db

# 파이썬
__pycache__/
*.pyc
.venv/

# 로컬 미리보기 산출물 (build_single.py 로 언제든 재생성)
미리보기.html

# 원본 압축
files.zip
